# forecast_function errors

    Code
      forecast_function(rtt_table = df, number_timesteps = 15, method = "linear",
        percent_change = 100)
    Condition
      Error in `match.arg()`:
      ! 'arg' should be one of "Linear", "Uniform"

